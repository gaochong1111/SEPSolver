#if !defined(TYPES_)
#define TYPES_

#include <vector>
#include <map>
#include <string>

using std::vector;
using std::map;
using std::string;

using SyntaxErrorTable = vector<string>;

using LogicInfo = map<string, string>;
using Theories = vector<string>;



#endif
